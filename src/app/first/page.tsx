// TODO; Remove `use client` when emotion support server components

'use client';

import { MemoriedFirst } from '@presentation/features/first';

function FirstPage(): JSX.Element {
  return <MemoriedFirst />;
}

export default FirstPage;
