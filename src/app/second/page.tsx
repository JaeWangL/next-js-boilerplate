// TODO; Remove `use client` when emotion support server components

'use client';

import { MemoriedSecond } from '@presentation/features/second';

function SecondPage(): JSX.Element {
  return <MemoriedSecond />;
}

export default SecondPage;
