import styled from '@emotion/styled';

export const FirstWrapper = styled.div({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  background: 'orange',
});
FirstWrapper.displayName = 'FirstWrapper';
